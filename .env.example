# PostgreSQL connection used by Prisma. Keep the real value server-side only.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require"

# Optional comma-separated origins allowed by custom deployments/proxies.
ALLOWED_ORIGINS="http://localhost:3000"
