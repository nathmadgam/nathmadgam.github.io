# Optional when testing a project-site subpath locally, for example /my-portfolio
NEXT_PUBLIC_BASE_PATH=""

# Public URL used for social metadata.
NEXT_PUBLIC_SITE_URL="https://nathmadgam.github.io"

# Optional public proxy origin for Roblox and Discord requests when browser CORS blocks them.
NEXT_PUBLIC_MEDIA_PROXY_BASE=""

# Required only for the optional Prisma/Node backend examples, not for GitHub Pages.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require"
