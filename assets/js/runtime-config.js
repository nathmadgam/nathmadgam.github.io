/**
 * Public runtime configuration. This file must never contain credentials.
 * Set this to the deployed optional media proxy origin when browser CORS rules
 * block direct public API requests, for example "https://media.example.workers.dev".
 */
globalThis.CYNEX_API_PROXY_BASE = "";
